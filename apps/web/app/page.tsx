import { AppShell } from "@/components/AppShell";

// Open directly on the Legal spoke to mirror the design-language blueprint (§5).
export default function Page() {
  return <AppShell initialSpoke="legal" />;
}
